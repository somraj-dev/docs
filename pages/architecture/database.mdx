# Database Architecture

Data modeling and persistence strategies.

## Primary Database
**PostgreSQL** is used for all persistent data including users, repositories, and configurations.

## Cache Layer
**Redis** is utilized for session storage and frequent API response caching to reduce latency.
